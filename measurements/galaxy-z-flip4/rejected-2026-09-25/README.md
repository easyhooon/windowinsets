# Rejected captures

`cover-gesture.json` and `cover-threeButton.json` were selected with Probe's manual Cover label while the Flip4 inner display remained active. Both report display 0 at 1080×2640 px and a FLAT folding feature, so they are not cover-screen evidence. Original log messages are preserved in `../rtl-logs/logs_2026-09-25.txt`; do not publish these values as cover insets.
